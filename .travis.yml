language: node_js

node_js:
  - "node"

install:
  - npm i -g npm
  - npm ci

script:
  - npm run lint
  - npm test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local_dir: build
  on:
    branch: develop
