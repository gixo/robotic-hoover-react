language: node_js

node_js:
  - "stable"

install:
  - npm install -g codecov

script:
  - npm test
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local_dir: build
  on:
    branch: test-deploy
